[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application (SPA) redirect rule
# This ensures that all requested paths are routed to index.html so React Router handles them client-side.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
